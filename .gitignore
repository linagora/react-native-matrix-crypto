node_modules/
target/
build/
Pods/
*.xcframework
*.aar
*.apk
.gradle/
.cxx/
jniLibs/
# The provenance stamps scripts/record-build-provenance.sh writes next to
# each build output: the commit that build had checked out, which
# scripts/measure-artifacts.sh requires to be the tree it is measuring. Each
# one describes one build on one machine, so a committed copy would be wrong
# for every checkout except the one that wrote it -- and wrong in the
# direction that matters, since a stamp naming this tree's HEAD is exactly
# what makes the gate accept a binary.
.build-provenance/
.DS_Store
# Not produced by the documented invocations, checked 2026-08-28: both Python
# programs here (packages/example-app/level-two/run_level_two.py and
# rust/matrix-crypto-core/tests/interop/nio_party.py) are run as scripts and
# import nothing local, and CPython does not cache __main__. Ignored anyway,
# because the day one of them grows a helper module, or someone runs pytest,
# is not the day to notice.
__pycache__/
# `npm pack` writes the tarball next to package.json unless told otherwise,
# and this one is 70 MB of prebuilt binaries. scripts/rehearse-publish.sh and
# the release workflow both pack into a temporary directory precisely so they
# never leave one here, but a hand-run `npm pack` while debugging a release
# does, and a 70 MB accidental commit is not undone by a revert.
*.tgz

# The design specs, implementation plans and measurement records that drove
# this library. Kept on disk and out of the repository for now, by the
# owner's decision.
#
# Two consequences worth knowing before relying on this. Tracked files still
# point here -- rust/matrix-crypto-core/src/observer.rs,
# scripts/measure-signal-latency.sh, scripts/assert-measure-guards.sh,
# packages/example-app/src/ProbeHarness.tsx and
# packages/react-native-matrix-crypto/interop/suite.ts all cite a path under
# docs/ -- so those references are now dangling for anyone reading the
# repository alone. And ignoring a directory does not unpublish it: every
# version already committed remains in this repository's history.
#
# The list is five files. It named README.md as a sixth from the day it was
# written and that was never true: `grep -n 'docs/' README.md` is empty now
# and was empty at 495a4bb, the commit that wrote this paragraph. A
# disclosure is worth having right, so it is corrected rather than trimmed
# to something vaguer that could not be wrong. The check is that grep, run
# against each file named above.
docs/

# Generated API reference (yarn docs; published to GitHub Pages by docs.yml)
api-docs/
